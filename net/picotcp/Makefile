ifeq ($(CONFIG_CPU_BIG_ENDIAN),y)
KBUILD_CPPFLAGS += -DPICO_BIGENDIAN
endif

ifeq ($(CONFIG_NET_PICO_SUPPORT_ETH),y)
KBUILD_CPPFLAGS += -DPICO_SUPPORT_ETH
endif

ifeq ($(CONFIG_NET_PICO_SUPPORT_DEVLOOP),y)
KBUILD_CPPFLAGS += -DPICO_SUPPORT_DEVLOOP
endif

ifeq ($(CONFIG_NET_PICO_SUPPORT_ICMP4),y)
KBUILD_CPPFLAGS += -DPICO_SUPPORT_ICMP4
KBUILD_CPPFLAGS += -DPICO_SUPPORT_PING
endif

ifeq ($(CONFIG_NET_PICO_SUPPORT_IPV4),y)
KBUILD_CPPFLAGS += -DPICO_SUPPORT_IPV4
endif

ifeq ($(CONFIG_NET_PICO_SUPPORT_UDP),y)
KBUILD_CPPFLAGS += -DPICO_SUPPORT_UDP
endif

#ifeq ($(CONFIG_NET_PICO_SUPPORT_TCP),y)
#KBUILD_CPPFLAGS += -DPICO_SUPPORT_TCP
#endif

ifeq ($(CONFIG_NET_PICO_SUPPORT_DHCP_CLIENT),y)
KBUILD_CPPFLAGS += -DPICO_SUPPORT_DHCPC
KBUILD_CPPFLAGS += -DPICO_SUPPORT_MCAST
KBUILD_CPPFLAGS += -DPICO_SUPPORT_IGMP
endif

obj-y += pico_rand.o

obj-y += commands/
obj-y += modules/
obj-y += stack/
