if MACH_MIPS_AR9331

config ARCH_TEXT_BASE
	hex
	default 0xa0800000

choice
	prompt "Board type"

config BOARD_TPLINK_WR720N
	bool "TP-LINK WR720N"
	select HAVE_PBL_IMAGE
	select HAVE_IMAGE_COMPRESSION
endchoice

endif

source arch/mips/boards/tplink-wr720n/Kconfig
